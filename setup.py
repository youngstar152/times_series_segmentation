from sys import version
from setuptools import setup, find_packages
setup(
    name="tspy",
    version="0.1",
    description="Lib for time series analysis.",
    author="Chengyu",
    packages=find_packages(),
)